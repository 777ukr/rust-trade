# 🧪 ТЕСТИРОВАНИЕ БЭКТЕСТА - Пошаговая инструкция

## Проблема
Бэктест запускается, но результаты не видны в UI.

## Что проверить

### 1. ✅ Логирование
Запустите с подробным логированием:
```bash
RUST_LOG=debug cargo run --bin investor_portal --features dashboard,database,gate_exec
```

### 2. ✅ Проверка данных
- **DATABASE_URL** установлен? → `echo $DATABASE_URL`
- Есть данные в БД? → Проверьте через `psql` или загрузите через `load_historical_data`
- Есть .bin файлы? → Проверьте `data/` директорию

### 3. ✅ Синтетические данные
Если данных нет - система автоматически генерирует синтетические данные для тестирования.

### 4. ✅ WebSocket соединение
- Откройте DevTools → Network → WS
- Проверьте подключение к `/api/backtest/:id/stream`
- Проверьте сообщения Progress/Complete/Error

### 5. ✅ API endpoints
- `GET /api/results` - должны быть результаты
- `GET /api/results/latest` - последние результаты
- `GET /api/trades/:backtest_id` - сделки

## Как должно работать

### Шаг 1: Запуск бэктеста
1. Выберите стратегию (mshot, mstrike, hook)
2. Выберите символы (SOL_USDT, ETH_USDT, BTC_USDT)
3. Выберите плечо (100x)
4. Нажмите "Запустить бэктест"

### Шаг 2: Обработка
1. Сервер получает запрос
2. Загружает данные (из БД или .bin, или генерирует синтетические)
3. Добавляет стратегию в движок
4. Запускает бэктест
5. Отправляет прогресс через WebSocket

### Шаг 3: Результаты
1. Бэктест завершается
2. Результаты сохраняются в `state.results`
3. Отправляется `Complete` сообщение через WebSocket
4. UI получает результаты и отображает их

## Проверка сейчас

### Проверка 1: Есть ли данные?
```bash
# Проверить DATABASE_URL
echo $DATABASE_URL

# Проверить .bin файлы
ls -la data/*.bin

# Если нет - генерируются синтетические автоматически
```

### Проверка 2: Логи
```bash
RUST_LOG=debug cargo run --bin investor_portal --features dashboard,database,gate_exec 2>&1 | grep -E "(Загрузка|Запуск|Бэктест|результат|Ошибка)"
```

### Проверка 3: API
```bash
# После запуска бэктеста проверить результаты
curl http://localhost:8080/api/results | jq
```

## Что исправлено

1. ✅ Добавлено детальное логирование на каждом этапе
2. ✅ Добавлена генерация синтетических данных если нет реальных
3. ✅ Улучшена обработка ошибок с понятными сообщениями
4. ✅ Проверка пустых потоков данных
5. ✅ Логирование количества результатов

## Следующие шаги

Если проблема остается:
1. Проверьте логи с `RUST_LOG=debug`
2. Проверьте WebSocket соединение в DevTools
3. Проверьте, что результаты сохраняются через `GET /api/results`
4. Проверьте UI - может быть проблема с отображением

