# 🎯 Полный план реализации системы для инвестора

## ✅ Что реализуем:

1. **Веб-портал** - выбор стратегий, плеча, запуск бэктестов
2. **EMA стратегия пользователя** - ловля разворотов на просадках (0.3%, 0.5%, 0.8%)
3. **Автоматический рейтинг стратегий** - фильтрация лучших конфигураций
4. **Учет комиссий с rebate** - 60% возврат Gate.io Futures
5. **База данных** - исторические данные + результаты бэктестов
6. **Визуализация** - графики, таблицы, метрики

## 🏗️ Архитектура

### Backend (Rust)
- `investor_portal.rs` - веб-сервер (Axum)
- `investor_demo.rs` - бэктест-раннер с поддержкой всех стратегий
- `utils/gate_commission.rs` - расчет комиссий с rebate
- `strategy/ema_reversal.rs` - ваша EMA стратегия с тройными ордерами

### Frontend
- `templates/investor_portal.html` - улучшенный UI
- WebSocket для прогресса бэктестов
- Chart.js для визуализации

### База данных
- PostgreSQL для результатов
- Исторические данные через Gate.io API

## 📋 Чеклист задач

### Этап 1: Базовая инфраструктура ✅
- [x] Веб-портал с выбором стратегий
- [x] Выбор плеча (3x-125x)
- [ ] Интеграция реальных бэктестов
- [ ] WebSocket для прогресса

### Этап 2: Стратегии
- [ ] EMA Reversal стратегия (ваша логика)
- [x] Long Trailing Stop
- [ ] Short Trailing Stop
- [ ] Улучшение существующих стратегий

### Этап 3: Комиссии и метрики
- [x] Модуль расчета комиссий с rebate
- [ ] Интеграция в бэктесты
- [ ] Автоматический рейтинг стратегий
- [ ] Фильтр по MaxDrawdown/Sharpe

### Этап 4: База данных
- [x] Схема PostgreSQL
- [ ] Скрипт загрузки исторических данных
- [ ] API для сохранения результатов

### Этап 5: UI/UX
- [x] Базовый интерфейс
- [ ] Прогресс-бар бэктеста
- [ ] Сравнение стратегий
- [ ] Экспорт отчетов

## 🚀 Команды запуска

```bash
# 1. Компиляция
cargo build --release --features gate_exec,database,dashboard

# 2. Настройка БД
createdb cryptotrader
psql cryptotrader < database/schema.sql

# 3. Загрузка исторических данных (скрипт будет создан)
python scripts/load_historical_data.py --symbols BTC_USDT ETH_USDT SOL_USDT --days 180

# 4. Запуск портала
cargo run --bin investor_portal --features dashboard

# 5. Откройте http://localhost:8080
```

## ⚠️ Важные замечания

1. **Не обещаем 100% прибыльность** - показываем только лучшие конфигурации по историческим данным
2. **Высокое плечо = высокий риск** - показываем предупреждение при >50x
3. **Реальная торговля требует KYC и юридической консультации**
4. **Все стратегии могут показывать минус в определенных условиях**

## 📊 Метрики для рейтинга стратегий

- ROI (чем выше, тем лучше)
- Sharpe Ratio (минимум 1.5)
- Max Drawdown (максимум 20%)
- Win Rate (минимум 55%)
- Profit Factor (минимум 1.5)
- Количество сделок (минимум 10 для статистики)

## 🔧 Конфигурация

Все параметры в `.env`:
```bash
DATABASE_URL=postgresql://user:pass@localhost:5432/cryptotrader
GATEIO_API_KEY=your_key
GATEIO_SECRET_KEY=your_secret
COMMISSION_REBATE_RATE=0.60  # 60% возврат
```



