# 🌐 Веб-дашборд для инвестора

## 🚀 Быстрый старт

### Запуск дашборда:
```bash
cargo run --bin dashboard_server --features dashboard
```

### Доступ к дашборду:
- **Локально:** http://localhost:8080
- **По сети:** http://<ваш-ip>:8080

### Как узнать свой IP:
```bash
# Linux
hostname -I
# или
ip addr show

# Покажет IP типа: 192.168.1.100
# Тогда доступ: http://192.168.1.100:8080
```

## 📊 Что показывает дашборд

### 1. Статистика (Stats Cards)
- Total P&L - общая прибыль/убыток
- Total Trades - количество сделок
- Win Rate - процент прибыльных сделок
- Wins/Losses - количество прибылей/убытков
- Profit Factor - фактор прибыли
- Avg Win/Loss - средние сделки
- Max Drawdown - максимальная просадка

### 2. Графики (Charts)
- **Price History** - график цен за период
- **P&L Over Time** - кумулятивная прибыль по времени
- **Win/Loss Distribution** - распределение прибылей/убытков

### 3. Таблица сделок (Trades Table)
- Последние 20 сделок
- Детали: вход, выход, сторона, P&L

## 🔧 Настройка

### Изменение порта:
Отредактируйте в `src/bin/dashboard_server.rs`:
```rust
let addr = "0.0.0.0:8080"; // Измените порт
```

### Выбор файла:
В дашборде есть выпадающий список для выбора файла результатов.

## 📁 Файлы

Дашборд автоматически ищет файлы в `data/`:
- `*_backtest.csv` - результаты торговли
- `*_prices.csv` - история цен

## 🔒 Безопасность

⚠️ **Внимание:** Дашборд слушает на `0.0.0.0` - доступен всем в сети!

Для production:
1. Используйте reverse proxy (nginx)
2. Добавьте авторизацию
3. Используйте HTTPS

## 🌐 Доступ из интернета (без домена)

### Вариант 1: Прямой доступ
1. Узнайте внешний IP сервера
2. Откройте порт 8080 в firewall
3. Доступ: `http://<external-ip>:8080`

⚠️ Небезопасно для production!

### Вариант 2: Через SSH туннель (рекомендуется)
```bash
# На клиентской машине
ssh -L 8080:localhost:8080 user@server-ip

# Затем открыть: http://localhost:8080
```

### Вариант 3: Cloudflare Tunnel (безопасно)
1. Установите cloudflared
2. Создайте туннель
3. Получите URL типа: `https://dashboard-xyz.trycloudflare.com`

## 📱 Мобильная версия

Дашборд адаптивен и работает на мобильных устройствах!

## 🔄 Автообновление

Дашборд автоматически обновляется каждые 30 секунд.

## 💡 Примеры использования

### Запуск на сервере:
```bash
# В screen/tmux сессии
screen -S dashboard
cargo run --bin dashboard_server --features dashboard
# Ctrl+A, D для отсоединения
```

### Логи:
Все логи выводятся в консоль. Для production используйте systemd service.

## 🎯 Следующие шаги

1. ✅ Запустите бэктест: `cargo run --bin sol_backtest`
2. ✅ Запустите дашборд: `cargo run --bin dashboard_server --features dashboard`
3. ✅ Откройте в браузере: http://localhost:8080
4. ✅ Покажите инвестору: http://<ваш-ip>:8080

## 📊 Скриншоты функций

Дашборд показывает:
- 📈 Графики цен в реальном времени
- 💰 Статистику производительности
- 📋 Детали всех сделок
- 🎯 Ключевые метрики для инвестора

