# 🎯 Краткая шпаргалка для инвестора

## 📊 Быстрый просмотр результатов

### 1. Просмотр всех файлов:
```bash
cargo run --bin view_results -- --all
```

### 2. Последний бэктест:
```bash
cargo run --bin view_results
```

### 3. Конкретный файл:
```bash
cargo run --bin view_results -f sol_backtest.csv
```

## 📁 Где искать файлы

**Директория:** `data/`

**Типы файлов:**
- `*_prices.csv` - история цен
- `*_backtest.csv` - результаты торговли
- `strategy_report_*.txt` - анализ стратегии

## 🎯 Ключевые метрики

### ✅ Хорошо:
- **Win Rate** > 55%
- **Profit Factor** > 2.0
- **Sharpe Ratio** > 1.5
- **Max Drawdown** < 15%

### ⚠️ Нужно улучшение:
- Win Rate < 50%
- Profit Factor < 1.5
- Max Drawdown > 25%

## 📈 Что значит каждая метрика

**Total P&L** - общая прибыль/убыток в долларах

**Win Rate** - процент прибыльных сделок (чем выше, тем лучше)

**Profit Factor** - отношение прибыли к убыткам:
- < 1.0 = убыточно
- 1.0-1.5 = на грани
- > 1.5 = прибыльно
- > 2.0 = отлично

**Sharpe Ratio** - риск-скорректированная доходность:
- < 0 = плохо
- 0-1 = приемлемо
- > 1 = хорошо
- > 2 = отлично

**Max Drawdown** - максимальная просадка капитала:
- < 10% = отлично
- 10-20% = хорошо
- > 20% = рискованно
- > 30% = опасно

## 🔍 Интерпретация CSV файлов

### Формат backtest файла:
```
entry_time,entry_price,exit_time,exit_price,side,pnl,pnl_percent
```

**Колонки:**
- `entry_time` - время входа (Unix timestamp)
- `entry_price` - цена входа
- `exit_time` - время выхода
- `exit_price` - цена выхода
- `side` - "long" или "short"
- `pnl` - прибыль/убыток в $
- `pnl_percent` - прибыль/убыток в %

### Пример:
```
1704067200,100.00,1704068100,104.00,long,4.00,4.00
```
→ Покупка по $100, продажа по $104, прибыль $4 (4%)

## 📊 Открыть в Excel/Google Sheets

1. Скопируйте CSV файл
2. Откройте в Excel/Sheets
3. Используйте фильтры и графики
4. Анализируйте паттерны

## 🚀 Быстрые команды

```bash
# Просмотр последних результатов
alias view-results='cargo run --bin view_results'

# Запуск бэктеста
cargo run --bin sol_backtest

# Анализ стратегии
cargo run --bin analyze_strategy_log -- -l data/sol_backtest.csv
```

## ❓ Вопросы и ответы

**Q: Где находятся файлы?**
A: В директории `data/` проекта

**Q: Как часто обновляются?**
A: После каждого запуска бэктеста

**Q: Можно ли экспортировать в Excel?**
A: Да, CSV файлы открываются напрямую в Excel

**Q: Что делать если нет результатов?**
A: Запустите: `cargo run --bin sol_backtest`

