# 🚀 Gate.io Trading Guide - Бэктестинг, Paper Trading и Live Trading

Это руководство описывает, как использовать Gate.io для всех трех режимов торговли в rust-trade.

## 📋 Содержание

1. [Конфигурация](#конфигурация)
2. [Бэктестинг (Backtesting)](#бэктестинг-backtesting)
3. [Paper Trading (Бумажная торговля)](#paper-trading-бумажная-торговля)
4. [Live Trading (Живая торговля)](#live-trading-живая-торговля)
5. [Примеры использования](#примеры-использования)

## ⚙️ Конфигурация

### Настройка биржи

В файле `config/development.toml` добавьте секцию `[exchange]`:

```toml
[exchange]
# Exchange provider: "binance" or "gateio"
provider = "gateio"
```

### Полная конфигурация для Gate.io

```toml
# Trading pairs to monitor (Gate.io поддерживает тот же формат: BTCUSDT, ETHUSDT)
symbols = ["BTCUSDT", "ETHUSDT", "ADAUSDT"]

[exchange]
provider = "gateio"

[paper_trading]
enabled = true
strategy = "rsi"    # или "sma"
initial_capital = 10000.0

[database]
max_connections = 5
min_connections = 1
max_lifetime = 1800

[cache]
[cache.memory]
max_ticks_per_symbol = 1000
ttl_seconds = 300

[cache.redis]
pool_size = 10
ttl_seconds = 3600
max_ticks_per_symbol = 10000
```

## 🔬 Бэктестинг (Backtesting)

Бэктестинг использует исторические данные из базы данных. Для работы с Gate.io данными:

### Шаг 1: Сбор данных с Gate.io

Сначала соберите исторические данные с Gate.io в базу данных:

```bash
# Запустите live режим для сбора данных
cargo run live
```

Это будет собирать данные с Gate.io и сохранять их в базу данных.

### Шаг 2: Запуск бэктестинга

```bash
cargo run backtest
```

Интерактивный интерфейс позволит вам:

- Выбрать стратегию (RSI или SMA)
- Выбрать торговую пару (например, BTCUSDT)
- Указать количество записей для тестирования
- Установить начальный капитал
- Установить комиссию

### Пример вывода бэктестинга

```
🎯 TRADING CORE BACKTESTING SYSTEM
============================================================
📊 Loading data statistics...

📈 Available Data:
  Total Records: 150000
  Available Symbols: 3
  Earliest Data: 2024-01-01 00:00:00 UTC
  Latest Data: 2024-12-15 23:59:59 UTC

🎯 Available Strategies:
  1) SMA Strategy - Trading strategy based on short and long-term moving average crossover
  2) RSI Strategy - Trading strategy based on Relative Strength Index (RSI)

Select strategy (1-2): 2
✅ Selected Strategy: RSI Strategy

📊 Symbol Selection:
  1) BTCUSDT (50000 records)
  2) ETHUSDT (45000 records)
  3) ADAUSDT (55000 records)

Select symbol (1-3): 1
✅ Selected Symbol: BTCUSDT (50000 records available)

Enter number of records to backtest (default: 10000, max: 50000): 10000
Enter initial capital (default: $10000): $10000
Enter commission rate % (default: 0.1%): 0.1

🔍 Loading historical tick data: BTCUSDT latest 10000 records...
✅ Loaded 10000 tick data points
📅 Data range: 2024-11-01 00:00:00 to 2024-12-15 23:59:59

============================================================
Starting backtest...
Strategy: RSI Strategy
Initial capital: $10000
Data points: 10000
Commission rate: 0.1%
============================================================

📊 Backtest Results:
  Initial Capital: $10000.00
  Final Capital: $10500.00
  Total Return: 5.00%
  Total Trades: 45
  Winning Trades: 28 (62.22%)
  Losing Trades: 17 (37.78%)
  Max Drawdown: -2.50%
  Sharpe Ratio: 1.25
```

## 📝 Paper Trading (Бумажная торговля)

Paper Trading позволяет тестировать стратегии на реальных данных без риска потери денег.

### Запуск Paper Trading с Gate.io

```bash
cargo run live --paper-trading
```

Или просто:

```bash
cargo run live
```

(если `paper_trading.enabled = true` в конфиге)

### Что происходит

1. **Подключение к Gate.io WebSocket** - получение реальных данных о сделках
2. **Обработка тиков** - стратегия анализирует каждый тик
3. **Генерация сигналов** - стратегия генерирует BUY/SELL/HOLD сигналы
4. **Симуляция сделок** - виртуальное выполнение сделок без реальных денег
5. **Логирование** - все действия записываются в базу данных

### Пример вывода Paper Trading

```
🚀 Paper trading is now active! Watch for trading signals below...
📈 Strategy: RSI Strategy | Initial Capital: $10000
================================================================================
🎯 BUY BTCUSDT @ $45000.00 | Portfolio: $10000.00 | P&L: $0.00 (0.00%) | Position: 0.2222 | Cash: $0.00 | Trades: 1 | Cache: HIT (45μs) | Total: 120μs
🎯 SELL BTCUSDT @ $45500.00 | Portfolio: $10111.11 | P&L: $111.11 (1.11%) | Position: 0.0000 | Cash: $10111.11 | Trades: 2 | Cache: HIT (42μs) | Total: 115μs
📊 BTCUSDT @ $45400.00 | Portfolio: $10111.11 | P&L: $111.11 | Cache: ✓ (38μs)
```

### Мониторинг Paper Trading

Все сделки и сигналы записываются в таблицу `live_strategy_log` в базе данных. Вы можете запросить статистику:

```sql
SELECT 
    strategy_id,
    symbol,
    COUNT(*) as total_signals,
    SUM(CASE WHEN signal_type = 'BUY' THEN 1 ELSE 0 END) as buy_signals,
    SUM(CASE WHEN signal_type = 'SELL' THEN 1 ELSE 0 END) as sell_signals,
    AVG(portfolio_value) as avg_portfolio_value,
    MAX(total_pnl) as max_pnl
FROM live_strategy_log
WHERE strategy_id = 'RSI Strategy'
GROUP BY strategy_id, symbol;
```

## 💰 Live Trading (Живая торговля)

**⚠️ ВНИМАНИЕ**: Live Trading выполняет реальные сделки с реальными деньгами. Используйте с осторожностью!

### Текущая реализация

Текущая версия rust-trade поддерживает:

- ✅ Сбор данных с Gate.io в реальном времени
- ✅ Paper Trading (виртуальная торговля)
- ⚠️ Live Trading (реальная торговля) - требует дополнительной реализации API для размещения ордеров

### Сбор данных в реальном времени

Для сбора данных без торговли:

```bash
cargo run live
```

Это будет:

- Подключаться к Gate.io WebSocket
- Получать данные о сделках в реальном времени
- Сохранять данные в базу данных
- Кэшировать данные в Redis и памяти

### Логи

```
🚀 Starting Trading Core Application (Live Mode)
📋 Configuration loaded successfully
📊 Monitoring symbols: ["BTCUSDT", "ETHUSDT", "ADAUSDT"]
🔌 Exchange provider: gateio
🔌 Connecting to database...
✅ Database connection established
💾 Initializing cache...
✅ Cache initialized
📡 Initializing exchange connection...
🔌 Exchange provider: gateio
✅ Exchange connection ready
🎯 Starting market data collection for 3 symbols
Connecting to Gate.io WebSocket with 3 channels
Subscription sent for 3 channels
```

## 📚 Примеры использования

### Пример 1: Бэктестинг стратегии RSI на данных Gate.io

1. Убедитесь, что в базе есть данные с Gate.io
2. Запустите бэктестинг:

   ```bash
   cargo run backtest
   ```

3. Выберите:
   - Strategy: 2 (RSI Strategy)
   - Symbol: BTCUSDT
   - Records: 10000
   - Initial Capital: $10000
   - Commission: 0.1%

### Пример 2: Paper Trading с Gate.io

1. Настройте `config/development.toml`:

   ```toml
   [exchange]
   provider = "gateio"
   
   [paper_trading]
   enabled = true
   strategy = "sma"
   initial_capital = 5000.0
   ```

2. Запустите:

   ```bash
   cargo run live --paper-trading
   ```

3. Наблюдайте за сигналами в реальном времени

### Пример 3: Переключение между биржами

Для переключения между Binance и Gate.io просто измените конфигурацию:

```toml
# Для Gate.io
[exchange]
provider = "gateio"

# Для Binance
[exchange]
provider = "binance"
```

Перезапустите приложение - все режимы (live, paper trading, data collection) будут использовать выбранную биржу.

## 🔧 Технические детали

### Формат символов

Gate.io использует тот же формат символов, что и Binance:

- `BTCUSDT` (автоматически конвертируется в `BTC_USDT` для WebSocket)
- `ETHUSDT`
- `ADAUSDT`

### WebSocket подключение

Gate.io WebSocket URL: `wss://fx-ws.gateio.ws/v4/ws/usdt`

Формат каналов: `futures.trades.BTC_USDT`

### REST API

Gate.io REST API URL: `https://api.gateio.ws/api/v4`

Эндпоинт для исторических данных: `/futures/usdt/trades?contract=BTC_USDT`

### Ограничения

- Gate.io имеет лимиты на количество символов в одной подписке
- Рекомендуется подписываться на не более 10-20 символов одновременно
- Rate limits: проверьте документацию Gate.io для актуальных лимитов

## 📖 Дополнительные ресурсы

- [Gate.io Futures API Docs](https://www.gate.io/docs/developers/apiv4/en/#futures)
- [Gate.io WebSocket Docs](https://www.gate.io/docs/developers/apiv4/en/#futures-trades)
- [Полная инструкция Gate.io интеграции](GATEIO_INTEGRATION.md)

## 📊 Просмотр отчетов

**Где смотреть отчеты:**

1. **Бэктестинг** - все отчеты отображаются прямо в консоли при запуске `cargo run backtest`
2. **Paper Trading** - сигналы в реальном времени в консоли + история в базе данных (`live_strategy_log`)
3. **Live Trading** - логи подключения в консоли + данные в базе (`tick_data`)

**Подробное руководство:** См. [REPORTS_GUIDE.md](REPORTS_GUIDE.md) для полной информации о том, где и как просматривать все отчеты, SQL запросы для анализа и экспорт данных.

## ❓ FAQ

**Q: Можно ли использовать Gate.io для бэктестинга?**  
A: Да! Бэктестинг использует данные из базы данных. Просто соберите данные с Gate.io, и они будут доступны для бэктестинга.

**Q: Нужно ли менять код для переключения между биржами?**  
A: Нет! Просто измените `provider` в конфигурации и перезапустите приложение.

**Q: Поддерживается ли реальная торговля (live trading) с Gate.io?**  
A: Текущая версия поддерживает сбор данных и paper trading. Для реальной торговли требуется дополнительная реализация API для размещения ордеров.

**Q: Можно ли использовать несколько бирж одновременно?**  
A: В текущей версии можно использовать только одну биржу за раз. Для использования нескольких бирж требуется дополнительная разработка.

**Q: Где я вижу результаты paper trading?**  
A: Результаты отображаются в реальном времени в консоли при запуске. Также все данные сохраняются в таблицу `live_strategy_log` в базе данных. См. [REPORTS_GUIDE.md](REPORTS_GUIDE.md) для SQL запросов.

**Q: Как сохранить отчеты в файл?**  
A: Используйте перенаправление вывода: `cargo run backtest 2>&1 | tee report.txt` или `cargo run live --paper-trading 2>&1 | tee paper_trading.log`

---

**Готово! Теперь вы можете использовать Gate.io для бэктестинга, paper trading и сбора данных в реальном времени! 🎉**
