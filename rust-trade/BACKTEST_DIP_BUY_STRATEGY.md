# 🔬 Бэктестинг Dip Buy Strategy для ETH

## 📋 Где отображается стратегия

### 1. В терминале (CLI)

При запуске бэктестинга:

```bash
cd trading-core
cargo run backtest
```

Вы увидите:

- Список доступных стратегий (включая "Dip Buy Strategy (Low Frequency)")
- Результаты бэктестинга с метриками
- Детальный анализ сделок

### 2. В веб-интерфейсе

После исправления ошибки Tauri API:

- Откройте `http://localhost:3000/backtest`
- Выберите стратегию "Dip Buy Strategy (Low Frequency)"
- Выберите символ ETHUSDT
- Запустите бэктестинг
- Просмотрите графики и таблицы

---

## 📥 Загрузка исторических данных для бэктестинга

### Вариант 1: Использование Premium Data Provider (Kaiko)

```bash
cd /home/crypto/sites/cryptotrader.com/rust-trade/scripts
python3 import_freqtrade_data.py --download --days 30 --symbol ETHUSDT
```

Этот скрипт:

1. Использует `/home/crypto/sites/cryptotrader.com/freqtrade/premium_data_provider.py`
2. Скачивает данные ETH 1m с Kaiko
3. Конвертирует OHLCV в tick data
4. Импортирует в базу данных rust-trade

### Вариант 2: Импорт из существующего файла

```bash
cd /home/crypto/sites/cryptotrader.com/rust-trade/scripts
python3 import_freqtrade_data.py --file /path/to/ETH_USDT-1m.json --symbol ETHUSDT
```

### Вариант 3: Загрузка через Gate.io API

```bash
cd trading-core
cargo run live
```

Это будет собирать данные в реальном времени и сохранять в базу данных.

---

## 🧪 Тестирование стратегии на истории

### Шаг 1: Загрузите исторические данные

```bash
# Установите переменные окружения
export DATABASE_URL="postgresql://user:password@localhost/trading_core"
export KAIKO_API_KEY="ec47c618-04bb-4eff-a962-dad3fab8ca45"

# Загрузите данные
cd /home/crypto/sites/cryptotrader.com/rust-trade/scripts
python3 import_freqtrade_data.py --download --days 30
```

### Шаг 2: Запустите бэктестинг

#### Через терминал

```bash
cd trading-core
cargo run backtest
```

Интерактивный процесс:

1. Выберите стратегию: `3` (Dip Buy Strategy)
2. Выберите символ: `ETHUSDT`
3. Укажите количество записей (например, 10000)
4. Установите начальный капитал: `10000`
5. Установите комиссию: `0.1%`

#### Через веб-интерфейс

1. Откройте `http://localhost:3000/backtest`
2. Выберите стратегию "Dip Buy Strategy (Low Frequency)"
3. Выберите символ ETHUSDT
4. Настройте параметры
5. Запустите бэктестинг
6. Просмотрите результаты с графиками

### Шаг 3: Анализ результатов

Вы увидите:

- **Return %** - общая доходность
- **Total Trades** - количество сделок
- **Win Rate** - процент прибыльных сделок
- **Max Drawdown** - максимальная просадка
- **Sharpe Ratio** - коэффициент Шарпа
- **Profit Factor** - фактор прибыли

---

## 📊 Параметры стратегии

Текущие параметры (можно изменить в коде):

- **buy_dip_percent**: 0.2% (покупка при просадке)
- **take_profit_percent**: 0.6% (продажа при прибыли)
- **stop_loss_percent**: 0.22% (стоп-лосс)

---

## 🔧 Настройка скрипта импорта

### Требования

```bash
pip install psycopg2-binary pandas requests
```

### Переменные окружения

```bash
export DATABASE_URL="postgresql://user:password@localhost/trading_core"
export KAIKO_API_KEY="your_kaiko_key"  # Опционально
export COINAPI_API_KEY="your_coinapi_key"  # Опционально
```

---

## 📈 Пример использования

```bash
# 1. Загрузите данные за последние 30 дней
cd /home/crypto/sites/cryptotrader.com/rust-trade/scripts
python3 import_freqtrade_data.py --download --days 30

# 2. Запустите бэктестинг
cd ../trading-core
cargo run backtest

# 3. Выберите:
#    - Strategy: 3 (Dip Buy Strategy)
#    - Symbol: ETHUSDT
#    - Records: 10000
#    - Initial Capital: $10000
#    - Commission: 0.1%
```

---

## ⚠️ Важные замечания

1. **Формат данных**: Скрипт конвертирует OHLCV в tick data, создавая 4 тика на каждую свечу
2. **Символы**: Используйте формат `ETHUSDT` (без разделителя)
3. **База данных**: Убедитесь, что база данных запущена и доступна
4. **API ключи**: Kaiko API ключ уже в скрипте, но можно использовать свой

---

## 🐛 Решение проблем

### Проблема: "Module not found: premium_data_provider"

Убедитесь, что путь к freqtrade правильный:

```python
sys.path.insert(0, '/home/crypto/sites/cryptotrader.com/freqtrade')
```

### Проблема: "Database connection failed"

Проверьте DATABASE_URL:

```bash
export DATABASE_URL="postgresql://user:password@localhost/trading_core"
```

### Проблема: "No data imported"

Проверьте:

- Существуют ли данные в файле
- Правильный ли формат (Freqtrade JSON)
- Доступна ли база данных

---

**Готово! Теперь вы можете загрузить исторические данные и протестировать стратегию! 🚀**
