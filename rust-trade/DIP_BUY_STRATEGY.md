# 📊 Dip Buy Strategy - Низкочастотная стратегия для ETH на Gate.io

## 🎯 Описание стратегии

Низкочастотная стратегия для торговли ETH на Gate.io с параметрами:

- **Покупка**: При просадке цены на **0.2%** от локального максимума
- **Продажа (Take Profit)**: При росте цены на **0.6%** от цены покупки
- **Стоп-лосс**: При падении цены на **0.22%** от цены покупки

## ⚙️ Конфигурация

Стратегия уже настроена в `config/development.toml`:

```toml
symbols = ["ETHUSDT"]

[paper_trading]
enabled = true
strategy = "dip_buy"
initial_capital = 10000.0

[exchange]
provider = "gateio"
api_key = "d42fb9e346be8d3d9b5102803de3b5f2"
api_secret = "e106f78d027a6bac04f093e7d629c26855cde4c6a4d32742a96efa1f5ca0b2f2"
```

## 🚀 Запуск Paper Trading

### 1. Убедитесь, что база данных и Redis запущены

```bash
# Проверьте подключение к базе данных
psql $DATABASE_URL -c "SELECT 1;"

# Проверьте Redis (если используется)
redis-cli ping
```

### 2. Запустите Paper Trading

```bash
cd trading-core
cargo run live --paper-trading
```

Или просто:

```bash
cd trading-core
cargo run live
```

(если `paper_trading.enabled = true` в конфиге)

## 📊 Что вы увидите

### Стартовая информация

```
🚀 Paper trading is now active! Watch for trading signals below...
📈 Strategy: Dip Buy Strategy (Low Frequency) | Initial Capital: $10000
================================================================================
```

### Примеры сигналов

**Покупка при просадке 0.2%:**

```
🎯 BUY ETHUSDT @ $3000.00 | Portfolio: $10000.00 | P&L: $0.00 (0.00%) | Position: 3.3333 | Cash: $0.00 | Trades: 1
```

**Продажа при Take Profit 0.6%:**

```
🎯 SELL ETHUSDT @ $3018.00 | Portfolio: $10060.00 | P&L: $60.00 (0.60%) | Position: 0.0000 | Cash: $10060.00 | Trades: 2
```

**Продажа при Stop Loss 0.22%:**

```
🎯 SELL ETHUSDT @ $2993.40 | Portfolio: $9978.00 | P&L: $-22.00 (-0.22%) | Position: 0.0000 | Cash: $9978.00 | Trades: 2
```

## 🔧 Настройка параметров

Если нужно изменить параметры стратегии, можно создать файл с параметрами или изменить значения по умолчанию в коде.

Текущие значения по умолчанию:

- `buy_dip_percent`: 0.2%
- `take_profit_percent`: 0.6%
- `stop_loss_percent`: 0.22%

## 📈 Логика работы стратегии

1. **Отслеживание максимума**: Стратегия отслеживает локальный максимум цены
2. **Сигнал покупки**: Когда цена падает на 0.2% от максимума → BUY
3. **Отслеживание позиции**: После покупки отслеживается цена покупки
4. **Take Profit**: Если цена выросла на 0.6% от цены покупки → SELL
5. **Stop Loss**: Если цена упала на 0.22% от цены покупки → SELL

## 💾 Просмотр результатов

### В реальном времени

Все сигналы отображаются в консоли в реальном времени.

### История из базы данных

```sql
-- Последние сделки
SELECT 
    timestamp,
    symbol,
    signal_type,
    current_price,
    portfolio_value,
    total_pnl,
    ROUND((total_pnl / 10000.0) * 100, 2) as return_pct
FROM live_strategy_log
WHERE strategy_id = 'Dip Buy Strategy (Low Frequency)'
  AND signal_type IN ('BUY', 'SELL')
ORDER BY timestamp DESC
LIMIT 50;
```

### Статистика

```sql
SELECT 
    COUNT(*) as total_signals,
    SUM(CASE WHEN signal_type = 'BUY' THEN 1 ELSE 0 END) as buys,
    SUM(CASE WHEN signal_type = 'SELL' THEN 1 ELSE 0 END) as sells,
    MAX(total_pnl) as max_pnl,
    MIN(total_pnl) as min_pnl,
    AVG(portfolio_value) as avg_portfolio
FROM live_strategy_log
WHERE strategy_id = 'Dip Buy Strategy (Low Frequency)';
```

## ⚠️ Важные замечания

1. **Низкочастотная стратегия**: Стратегия не торгует на каждом тике, а ждет значительных движений (0.2%+)
2. **Только ETH**: Стратегия настроена только на ETHUSDT
3. **Paper Trading**: Это виртуальная торговля, реальные деньги не используются
4. **API ключи**: Ключи сохранены в конфиге, но пока используются только для чтения данных через WebSocket

## 🔄 Остановка

Нажмите `Ctrl+C` для остановки. Все данные сохранятся в базу данных.

## 📚 Дополнительная информация

- [Полное руководство по отчетам](REPORTS_GUIDE.md)
- [Руководство по Gate.io](GATEIO_TRADING_GUIDE.md)

---

**Готово к запуску! 🚀**
