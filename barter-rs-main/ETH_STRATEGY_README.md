# Ethereum Dip Strategy для Barter

## Описание

Стратегия для торговли Ethereum (ETH) на бирже Gate.io с использованием фреймворка Barter на Rust.

### Параметры стратегии

- **Вход в позицию**: при просадке цены на 0.2% от локального максимума
- **Тейк-профит**: 0.6% от цены входа
- **Стоп-лосс**: 0.22% от цены входа

## Структура проекта

```
barter/examples/
├── eth_dip_strategy.rs      # Основной файл для запуска стратегии
├── strategy.rs              # Реализация стратегии
├── global_data.rs           # Глобальные данные стратегии
└── config/
    └── eth_gateio_config.json  # Конфигурация для Gate.io
```

## Запуск

### 1. Сборка проекта (рекомендуется release режим)

```bash
cd /home/crypto/sites/cryptotrader.com/barter-rs-main
cargo build --release --example eth_dip_strategy
```

**Примечание**: Release режим рекомендуется для лучшей производительности и меньшего потребления памяти при компиляции.

### 2. Запуск стратегии

```bash
cargo run --release --example eth_dip_strategy
```

Стратегия будет работать до нажатия Ctrl+C. После остановки будет выведена статистика торговли.

## Конфигурация

Конфигурация находится в файле `barter/examples/config/eth_gateio_config.json`:

- **Инструмент**: ETH_USDT на Gate.io Spot
- **Начальный баланс**: 10000 USDT
- **Комиссия**: 0.02% (0.0002)
- **Латентность**: 100ms (для mock execution)
- **Подписки**: PublicTrades (Gate.io Spot не поддерживает OrderBooksL1)

## Режим Paper Trading

По умолчанию стратегия использует **mock execution**, что означает:

- Безопасное тестирование без реальных средств
- Симуляция исполнения ордеров
- Отслеживание PnL и статистики

## API ключи Gate.io

Для реальной торговли необходимо настроить API ключи Gate.io в конфигурации execution.

**Важно**: Текущая конфигурация использует mock execution для безопасного тестирования.

## Мониторинг

Стратегия выводит логи через `tracing`:

- Входы в позиции
- Цены входа, тейк-профита и стоп-лосса
- Статистику торговли

## Дальнейшая настройка

1. **Реальное исполнение**: Настройте execution для подключения к Gate.io API
2. **Управление рисками**: Настройте RiskManager для дополнительных проверок
3. **Мониторинг**: Добавьте интеграцию с системами мониторинга

## Примечания

- Стратегия отслеживает локальный максимум цены для определения просадки
- Используется market order для входа в позицию
- Take profit и stop loss должны быть настроены на уровне биржи или через дополнительные ордера
- Gate.io Spot поддерживает только PublicTrades, поэтому OrderBooksL1 не используется
- Для остановки стратегии нажмите Ctrl+C - будет выведена статистика торговли

## Логи и Отчеты

### 📁 Где находятся файлы

- **Логи**: `logs/eth_strategy_YYYYMMDD_HHMMSS.log`
- **Отчеты**: `eth_strategy_summary_YYYYMMDD_HHMMSS.txt` (в корне проекта)

### 📊 Что логируется

- **В консоль**: основные события (INFO уровень)
- **В файл**: все детальные события (DEBUG уровень)
- **Отчет**: полная статистика после остановки стратегии

Подробнее см. [LOGS_AND_REPORTS.md](LOGS_AND_REPORTS.md)

### 🔍 Быстрый просмотр

```bash
# Просмотр логов в реальном времени
tail -f logs/eth_strategy_*.log

# Просмотр последнего отчета
cat $(ls -t eth_strategy_summary_*.txt | head -1)
```

## Статус

✅ **Стратегия готова к использованию!**

- Код компилируется успешно
- Все зависимости настроены
- Mock execution работает
- Логирование настроено (консоль + файл)
- Отчеты сохраняются автоматически
- Готова к подключению реального API Gate.io
