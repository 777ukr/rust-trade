# 📊 Логи и Отчеты Ethereum Dip Strategy

## 📁 Расположение файлов

### Логи (Logs)

**Директория**: `logs/`

**Формат имени файла**: `eth_strategy_YYYYMMDD_HHMMSS.log`

**Пример**: `logs/eth_strategy_20251115_162530.log`

**Содержимое**:

- Все события стратегии (уровень DEBUG)
- Подключения к биржам
- Входы в позиции
- Цены и расчеты
- Ошибки и предупреждения

**Просмотр логов в реальном времени**:

```bash
tail -f logs/eth_strategy_*.log
```

**Просмотр последнего лога**:

```bash
ls -t logs/eth_strategy_*.log | head -1 | xargs tail -f
```

### Отчеты (Reports)

**Директория**: корневая директория проекта

**Формат имени файла**: `eth_strategy_summary_YYYYMMDD_HHMMSS.txt`

**Пример**: `eth_strategy_summary_20251115_162530.txt`

**Содержимое**:

- Полная статистика торговли
- PnL (прибыль/убыток)
- Количество сделок
- Sharpe ratio
- Sortino ratio
- Максимальная просадка (drawdown)
- И другие метрики производительности

**Просмотр последнего отчета**:

```bash
ls -t eth_strategy_summary_*.txt | head -1 | xargs cat
```

## 📋 Что логируется

### В консоль (INFO уровень)

- Запуск стратегии
- Параметры стратегии
- Путь к файлу логов
- Входы в позиции (✅ ENTERING LONG POSITION)
- Завершение работы
- Статистика торговли

### В файл (DEBUG уровень)

- Все события из консоли
- Детальная информация о ценах
- Расчеты просадки
- Состояние позиций
- Все события движка

## 🔍 Примеры использования

### Просмотр всех входов в позиции

```bash
grep "ENTERING LONG POSITION" logs/eth_strategy_*.log
```

### Просмотр статистики по цене входа

```bash
grep "entry_price" logs/eth_strategy_*.log
```

### Просмотр всех ошибок

```bash
grep -i "error\|warn" logs/eth_strategy_*.log
```

### Просмотр последнего отчета

```bash
cat $(ls -t eth_strategy_summary_*.txt | head -1)
```

## 📊 Формат логов

### Вход в позицию

```
INFO eth_dip_strategy::strategy: ✅ ENTERING LONG POSITION
    instrument: InstrumentIndex(0)
    entry_price: 2500.50
    take_profit: 2515.30
    stop_loss: 2494.99
    quantity: 3.99
    highest_price: 2505.51
    dip_percentage: 0.2
```

### Статистика в отчете

```
================================================================================
ETHEREUM DIP STRATEGY - TRADING SUMMARY
Generated: 2025-11-15 16:25:30 UTC
================================================================================

TradingSummary {
    time_engine_start: 2025-11-15T16:25:13Z,
    time_engine_end: 2025-11-15T16:30:45Z,
    instruments: {...},
    assets: {...}
}
```

## 🗂️ Управление файлами

### Очистка старых логов (старше 7 дней)

```bash
find logs/ -name "eth_strategy_*.log" -mtime +7 -delete
```

### Очистка старых отчетов (старше 7 дней)

```bash
find . -maxdepth 1 -name "eth_strategy_summary_*.txt" -mtime +7 -delete
```

### Архивирование логов

```bash
tar -czf logs_archive_$(date +%Y%m%d).tar.gz logs/
```

## 💡 Советы

1. **Мониторинг в реальном времени**: Используйте `tail -f` для отслеживания событий
2. **Поиск по логам**: Используйте `grep` для фильтрации нужных событий
3. **Анализ производительности**: Смотрите отчеты для оценки эффективности стратегии
4. **Резервное копирование**: Регулярно архивируйте логи и отчеты

## 📍 Полные пути

- **Логи**: `/home/crypto/sites/cryptotrader.com/barter-rs-main/logs/`
- **Отчеты**: `/home/crypto/sites/cryptotrader.com/barter-rs-main/`

## ⚠️ Важно

**Если стратегия уже запущена**, файлы логов создаются при старте. Если вы не видите файлы:

1. **Проверьте, что стратегия запущена из правильной директории**:

   ```bash
   cd /home/crypto/sites/cryptotrader.com/barter-rs-main
   cargo run --release --example eth_dip_strategy
   ```

2. **Проверьте путь к логам в выводе стратегии** - при запуске будет показан полный путь к файлу лога

3. **Используйте скрипт для просмотра**:

   ```bash
   ./view_logs.sh
   ```

4. **Если стратегия запущена в фоне**, логи могут быть в рабочей директории процесса
